# delete objects in the workspace
rm(list = ls())
example(mean) # run the example code for mean
save(xm, file = "xm object.rdata")
#' This is the first line
#' this is the second line
#'
rm(xm)
print(ls())
x
